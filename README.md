# ProberX
ProberX — 自托管服务器监控平台。支持 CPU/内存/磁盘/网络指标采集、HTTP/TCP/DNS/SSL/gRPC   多类型探活、告警规则引擎与多渠道通知（Email/Slack/Webhook）、Cron 定时任务调度、公开状态页。Go Agent 轻量部署，Fastify 后端 + Next.js   前端，PostgreSQL + TimescaleDB + Redis 存储栈。
